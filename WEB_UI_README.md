# MediaCrawler Web UI 说明文档

## 🎯 简介

MediaCrawler Web UI 是一个现代化的可视化控制面板，让您可以通过浏览器轻松管理和监控爬虫任务，无需使用命令行。

## ✨ 主要特性

### 1. 可视化控制面板
- 🎨 现代化的用户界面设计
- 📱 响应式布局，支持移动端
- 🌙 优雅的配色方案
- ⚡ 流畅的动画效果

### 2. 实时监控
- 📊 实时显示爬取进度
- 📈 动态更新统计数据
- 📝 实时日志查看器
- 🔄 自动刷新状态（每10秒）

### 3. 断点续爬
- 💾 自动保存爬取进度
- 🔄 支持中断后恢复
- 📋 查看所有历史检查点
- 🎯 一键恢复到任意检查点

### 4. 数据管理
- 📁 浏览所有数据文件
- 👀 在线预览数据内容
- 💾 下载数据到本地
- 🗑️ 删除不需要的文件

## 🚀 快速开始

### 启动服务

```bash
# 方式1：直接运行
uv run python api_server.py

# 方式2：使用启动脚本（Linux/Mac）
./start_web_ui.sh

# 方式3：使用启动脚本（Windows）
start_web_ui.bat
```

### 访问控制面板

服务启动后，在浏览器中访问：

- **控制面板**: http://localhost:8000
- **API 文档**: http://localhost:8000/docs
- **交互式文档**: http://localhost:8000/redoc

## 📖 界面说明

### 顶部导航栏

- **Logo 区域**: 显示项目名称和简介
- **刷新按钮**: 手动刷新所有状态
- **状态指示器**: 显示当前爬虫运行状态

### 爬虫控制面板

#### 平台选择
点击对应的平台卡片即可选择：
- 🔴 小红书 (xhs)
- 🎵 抖音 (dy)
- ⚡ 快手 (ks)
- 📺 哔哩哔哩 (bili)
- 🐦 微博 (wb)
- 💬 百度贴吧 (tieba)
- 🧠 知乎 (zhihu)

#### 配置参数

**关键词**
- 输入搜索关键词，多个关键词用逗号分隔
- 示例：`编程副业,自媒体运营,Python教程`

**爬取类型**
- 关键词搜索：按关键词搜索相关内容
- 帖子详情：爬取指定ID的帖子
- 创作者主页：爬取指定创作者的所有内容

**登录方式**
- 二维码登录：扫码登录（推荐）
- 短信验证码：手机号验证码登录
- Cookie 登录：使用已有的 Cookie

**数量控制**
- 最大帖子数：要爬取的帖子数量（1-500）
- 每帖最大评论数：每个帖子爬取的评论数（0-1000）

**功能开关**
- ✅ 爬取评论：是否获取评论数据
- ✅ 爬取二级评论：是否获取回复评论
- ✅ 断点续爬：启用断点续爬功能（推荐）
- ✅ 无头模式：后台运行，不显示浏览器

**数据存储**
- JSON 文件：保存为 JSON 格式（推荐）
- CSV 文件：保存为 CSV 格式
- SQLite 数据库：轻量级数据库
- MySQL 数据库：生产环境数据库

#### 操作按钮

- **启动爬虫**: 开始执行爬取任务
- **停止爬虫**: 停止当前任务并保存检查点

### 运行状态面板

实时显示爬虫的运行信息：

- **当前状态**: 运行中/空闲
- **开始时间**: 任务开始的时间
- **已爬帖子**: 已成功爬取的帖子数
- **已爬评论**: 已成功爬取的评论数
- **进度条**: 可视化进度显示
- **日志查看器**: 实时显示运行日志

### 断点续爬面板

管理所有保存的检查点：

- **检查点列表**: 显示所有历史检查点
- **检查点信息**: 
  - ID：唯一标识符
  - 平台：爬取的平台名称
  - 时间：创建时间
- **操作按钮**:
  - 恢复：从该检查点继续爬取
  - 下载：下载检查点文件

### 数据中心面板

浏览和管理爬取的数据：

- **文件列表**: 显示所有数据文件
  - 文件名
  - 文件大小
  - 修改时间
- **筛选选项**: 按文件类型筛选（JSON/CSV）
- **操作按钮**:
  - 预览：在线查看数据内容
  - 下载：下载文件到本地
  - 删除：删除不需要的文件

## 💡 使用技巧

### 1. 快速开始爬取

1. 选择平台（如小红书）
2. 输入关键词（如"编程副业"）
3. 保持默认配置
4. 点击"启动爬虫"
5. 在状态面板查看进度

### 2. 使用断点续爬

场景：爬虫意外中断，需要继续

1. 查看"断点续爬"面板
2. 找到最新的检查点
3. 点击"恢复"按钮
4. 爬虫将从中断处继续

### 3. 数据预览和下载

1. 切换到"数据中心"
2. 找到想要的数据文件
3. 点击"预览"查看内容
4. 点击"下载"保存到本地

### 4. 批量爬取

需要爬取多个关键词：

1. 启动第一个关键词的爬取
2. 等待完成（查看状态面板）
3. 更改关键词
4. 启动下一个爬取任务

### 5. 监控长时间任务

1. 启动爬取任务
2. 页面会每10秒自动刷新状态
3. 可以最小化浏览器，稍后再查看
4. 任务完成后状态会自动更新

## 🎨 界面定制

### 修改主题颜色

编辑 `web_ui/static/styles.css`：

```css
:root {
    --primary: #3b82f6;        /* 主色调 */
    --primary-dark: #2563eb;   /* 主色调（深）*/
    --success: #10b981;        /* 成功色 */
    --warning: #f59e0b;        /* 警告色 */
    --danger: #ef4444;         /* 危险色 */
}
```

### 添加自定义样式

在 `styles.css` 末尾添加：

```css
/* 自定义样式 */
.my-custom-class {
    /* 您的样式 */
}
```

### 修改功能

编辑 `web_ui/static/main.js` 添加自定义逻辑。

## 🔧 故障排除

### 问题1：无法访问 Web UI

**症状**: 浏览器显示"无法连接"

**解决方案**:
1. 确认服务是否正常启动
2. 检查端口 8000 是否被占用
3. 尝试访问 http://127.0.0.1:8000

### 问题2：启动爬虫失败

**症状**: 点击"启动爬虫"后显示错误

**解决方案**:
1. 查看日志查看器中的错误信息
2. 确认配置参数是否正确
3. 检查是否已有爬虫在运行

### 问题3：数据文件不显示

**症状**: 数据中心为空

**解决方案**:
1. 确认是否已完成爬取任务
2. 检查 `data/` 目录是否存在数据文件
3. 点击"刷新状态"按钮

### 问题4：检查点无法恢复

**症状**: 点击"恢复"无效

**解决方案**:
1. 确认没有其他爬虫任务正在运行
2. 检查检查点文件是否完整
3. 查看浏览器控制台的错误信息

## 🔒 安全建议

### 本地开发环境

默认配置已足够安全，只允许本地访问。

### 生产环境部署

如果需要在服务器上部署：

1. **启用身份验证**
   - 添加登录功能
   - 使用 JWT Token

2. **使用 HTTPS**
   - 配置 SSL 证书
   - 强制使用 HTTPS

3. **限制访问**
   - 配置防火墙
   - 使用 Nginx 反向代理
   - 限制允许的 IP 地址

4. **环境变量**
   - 不要在代码中硬编码敏感信息
   - 使用环境变量存储配置

示例 Nginx 配置：

```nginx
server {
    listen 443 ssl;
    server_name your-domain.com;

    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## 📱 移动端使用

Web UI 支持移动端访问：

1. 确保手机和服务器在同一网络
2. 在手机浏览器访问：`http://服务器IP:8000`
3. 建议添加到主屏幕以获得类似 App 的体验

## 🔮 未来计划

- [ ] WebSocket 实时日志推送
- [ ] 用户认证系统
- [ ] 多任务并发支持
- [ ] 数据可视化图表
- [ ] 定时任务调度
- [ ] 邮件通知功能
- [ ] 导出报告功能
- [ ] 主题切换（亮色/暗色）

## 💬 反馈与支持

如遇到问题或有建议，请：
- 查看 [API_GUIDE.md](API_GUIDE.md)
- 查看 [EXAMPLE_USAGE.md](EXAMPLE_USAGE.md)
- 提交 GitHub Issue
- 加入微信交流群

---

**享受现代化的爬虫体验！** 🎉
